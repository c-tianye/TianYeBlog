{
	"name": "tianye-digest",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"description": "Scheduled crawler that summarises tech sources with Gemini and commits bilingual digest posts into the blog",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"test": "node --test test/*.test.ts",
		"typecheck": "tsc --noEmit",
		"tail": "wrangler tail",
		"types": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260301.0",
		"typescript": "^6.0.3",
		"wrangler": "^4.136.3"
	}
}
