---
title: "Pi Releases 2026-09-24 10:02"
description: "Pi version 0.86.0 is now live, introducing prompt cache warming, bug reporting, offline Radius model catalogs, per-model compaction budgets, and critical breaking changes."
publishDate: "2026-09-24T10:02:00+08:00"
tags: ["digest","pi"]
draft: false
---

## Versions overview

This release brings Pi 0.86.0, focusing heavily on prompt cache optimizations, session diagnostics enhancements, and greater flexibility in model configurations.

## Version by version

### Pi 0.86.0 (2026-09-19)

**New** — Introduced prompt cache warming to keep valuable prompt caches alive during long tool runs and optionally while idle using cost-aware refreshes. See [Cache Warming](https://pi.dev/docs/latest/settings#cache-warming). Added bug reporting with /bug using redacted diagnostics, optional transcripts, or exported ZIP archives. See [Reporting Bugs](https://pi.dev/docs/latest/sessions#reporting-bugs). Enabled transcript-aware prompt and tool updates to preserve instruction and tool changes across resume and branch navigation while retaining cached prefixes. See [before_agent_start](https://pi.dev/docs/latest/extensions#before_agent_start). Added offline Radius model catalog for immediate Radius model selection with cached and live catalogs overlaid when available. See [Radius](https://pi.dev/docs/latest/providers#radius). Added per-model compaction budgets to configure reserved and recent-token budgets by model. See [Per-model overrides](https://pi.dev/docs/latest/compaction#per-model-overrides).

**Added** — Added transcript-backed mid-conversation system prompt and tool changes (#9548). See [Entry Types](https://pi.dev/docs/latest/session-format#entry-types) and [PR 9548](https://github.com/earendil-works/pi/pull/9548). Added inherited native deferred tool loading for Fireworks Messages models (#9323). Added click toggling for branch summaries, compaction summaries, and skill invocation entries. Added the public Radius model catalog for immediate and offline model selection. Added `ctx.modelRegistry.stream()` and `streamSimple()` for extension model calls (#8964). Added per-model reserveTokens and keepRecentTokens settings through `compaction.modelOverrides` (#8133). Added `compat.allowedFallbackModels` configuration for overriding or disabling Anthropic server-side fallback models (#9294). Added an unsubscribe function from `pi.on()` for extensions to drop event handlers (#8967). Exported extension hook event and result types previously omitted from package entry points (#9642). Added `/bug [description]` to report bugs to the Pi developers.

**Breaking** — Changed inherited pi-ai provider stream inputs from Context to normalized TranscriptContext values; custom providers must read system prompts and tool declarations from context.messages with `getCurrentSystemPrompt()` and `getCurrentTools()`. See [Custom Streaming API](https://pi.dev/docs/latest/custom-provider#custom-streaming-api). Restricted inherited `ToolCall.arguments` and `ToolResultMessage.details` to JSON-compatible values, changed `ToolResultMessage` into a conditional type, and made `JsonValue` arrays readonly. `user_bash` now fails closed: errors or invalid defined results abort the command without invoking later handlers or executing locally (#9068).

## Upgrade notes

This release includes key breaking changes such as custom streaming API modifications and user_bash fail-closed policies. Please thoroughly check custom provider implementations and scripts before upgrading.

---

**Sources** (1 items): [Pi releases](https://pi.dev/changelog)（1）

Collected 2026-09-24 10:02 CST by a Cloudflare Worker, summarised by Gemini.
Machine-generated digest — the [Chinese version](https://blog.luxstarspace.com/posts/digest-pi-20260924-1002/) may read better.
